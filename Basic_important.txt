prompt();
const password=prompt("ENter your password");
This prompt() function allow user to enter inputs in runtime.
================================================
for loop control through an array.
const hobbies=["sport", "dance","work"];
for(const hobby of hobbies){
  console.log(hobby); // will execute this line three times
}
================================================
27.
we dont do dom manipulation in reactJs.

const list= document.querySelector("ul")
list.remove();
